import { EN, AR } from "@i18n/messages";

export const transOptions = {
    resources: {
        en: EN,
        ar: AR,
    },
    lng: "en",
    fallbackLng: "en",
    interpolation: { escapeValue: false },
};
